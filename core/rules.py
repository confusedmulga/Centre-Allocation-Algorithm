def apply_priority_rules(students_df, params):
    # TODO: sort by disability > female > others
    return students_df.copy()

def apply_distance_rules(student, centers_df, params):
    # TODO: filter centers by distance and other rules
    return centers_df.copy()
